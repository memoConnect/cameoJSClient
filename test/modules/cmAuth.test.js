'use strict';

describe('cmAuth', function(){

	beforeEach(module('cmAuth'))

	beforeEach(inject(function(_cmAuth_){

	}))

	afterEach(function(){				
		
	})

	/*
	starting any test with this setup causes a minErr ?!
	blame the inject above, without to error occurs.

	it('should provide a service for authentication tasks', function(){
		//expect(cmAuth).toBeDefined()
	})
	*/


})