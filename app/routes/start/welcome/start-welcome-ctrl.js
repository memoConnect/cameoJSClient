define([
    'app',
    'ngload!pckCore',
    'ngload!pckUi',
    'ngload!pckUser',
    'ngload!pckWidgets'
], function (app) {
    'use strict';

    app.register.controller('StartWelcomeCtrl', [
        // no dependencies
        function() {
            // nothing to do
        }
    ]);
});