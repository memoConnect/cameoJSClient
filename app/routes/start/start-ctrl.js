define([
    'app',
    'ngload!pckCore',
    'ngload!pckUi',
    'ngload!pckUser',
    'ngload!pckWidgets',
], function (app) {
    'use strict';

    app.register.controller('StartCtrl', [
        // no dependencies
        function() {
            // nothing to do
        }
    ]);
});