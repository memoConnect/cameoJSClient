define([
    'app',
    'ngload!pckCore',
    'ngload!pckWidgets'
], function (app) {
    'use strict';

    app.register.controller('AuthenticationCtrl', [
        // no depedencies
        function() {
            // nothing to do here
        }
    ]);
});