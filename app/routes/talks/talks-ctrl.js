define([

    'app',
    'ngload!pckFiles',
    'ngload!pckUser',
    'ngload!pckContacts',
    'ngload!pckConversations',
    'ngload!pckWidgets',

], function (app) {
    'use strict';

    app.register.controller('ConversationsCtrl',[

        // no dependencies

        function() {
           //nothing to here yet
        }
    ]);
});