define([
    'app',
    'ngload!pckCore',
    'ngload!pckWidgets'
], function (app) {
    'use strict';

    app.register.controller('SystemcheckCtrl', [
        // no dependencies
        function() {
            // nothing to do here
        }
    ]);
});