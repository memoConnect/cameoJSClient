define([
    'app',
    'ngload!pckCore',
    'ngload!pckUi',
    'ngload!pckUser',
    'ngload!pckWidgets',
    'ngload!pckValidate'
], function (app) {
    'use strict';

    app.register.controller('SettingsIdentityEditCtrl', [
        // no dependencies
        function() {
            //nothing to here yet
        }
    ]);
});