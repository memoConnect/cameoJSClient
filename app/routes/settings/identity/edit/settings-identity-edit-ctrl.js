define([
    'app',
    'ngload!pckCore',
    'ngload!pckUi',
    'ngload!pckUser',
    'ngload!pckWidgets',
    'ngload!pckValidate',
    'ngload!pckFiles'
], function (app) {
    'use strict';

    app.register.controller('SettingsIdentityEditCtrl', [
        // no dependencies
        function() {
            //nothing to here yet
        }
    ]);
});