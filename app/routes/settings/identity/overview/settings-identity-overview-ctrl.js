define([
    'app',
    'ngload!pckCore',
    'ngload!pckUi',
    'ngload!pckUser',
], function (app) {
    'use strict';

    app.register.controller('SettingsIdentityOverviewCtrl', [
        // no dependencies
        function($scope) {
            //nothing to here yet
        }
    ]);
});