'use strict';

angular.module('cmFiles').directive('cmAssetPreview',[
    function () {
        return {
            restrict: 'E',
            controller: function ($scope, $element, $attrs) {

            }
        }
    }
]);