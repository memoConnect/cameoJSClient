'use strict';

angular.module('cmUser').directive('cmAutocomplete', [
    function () {
        return {
            restrict: 'A',
            link: function (scope, element, attrs) {

            }
        }
    }
]);