cameoJSClient
==================

cameo Web / MobileWeb / Hybrid client


#### env.js
Example for local API

    define([
        'app'
    ],function (app) {
        'use strict';
        app.cameo.restApi = "http://localhost:9000/api/v1";
    });

